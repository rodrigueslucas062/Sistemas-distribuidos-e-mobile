#Faça um programa que calcule e mostre a área de um triângulo. Sabe-se que:

base = int(input('Base: '))
altura = int(input('Altura: '))

area = (base * altura) / 2 
print('A área é: {:.1f}'.format(area))